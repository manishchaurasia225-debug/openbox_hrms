# Base URL for the HRMS API.
# Dev: leave as /api/v1 — the Vite dev server proxies it to the backend on :8080.
# Prod: set to the deployed API origin, e.g. https://hrms.example.com/api/v1
VITE_API_BASE_URL=/api/v1
